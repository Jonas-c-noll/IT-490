#!/usr/bin/env python
import pika
import json
import call

def func(user):
    s = "SELECT `Friends` FROM `Auth` WHERE `username` = '"+ user + "';"
    #execute the script here

def insrt(user):
    s = 
